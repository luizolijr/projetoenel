{% extends 'base.html.twig' %}

{% block title %}Lingua{% endblock %}

{% block body %}
    <div class="page-content">
            <div class="page-subheading page-subheading-md">
                <ol class="breadcrumb">
                    <li class="active">
                        <a href="javascript:;">Língua</a>
                    </li>
                </ol>
            </div>

        <div class="container-fluid-md">
            <a class="btn btn-inverse" href="{{ path('lingua_index') }}">Voltar</a>            

            <a class=" btn btn-primary" href="{{ path('lingua_edit', {'id': lingua.id}) }}">Editar</a>            
            <br></br>
            {{ include('lingua/_delete_form.html.twig') }}

            

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lingua.id }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ lingua.descricao }}</td>
            </tr>
            <tr>
                <th>Nivel_fluencia</th>
                <td>{{ lingua.nivelFluencia }}</td>
            </tr>
        </tbody>
    </table>
    
    </div>


{% endblock %}
