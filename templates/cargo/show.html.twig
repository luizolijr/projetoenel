{% extends 'base.html.twig' %}

{% block title %}Cargo{% endblock %}

{% block body %}
    <div class="page-content">
            <div class="page-subheading page-subheading-md">
                <ol class="breadcrumb">
                    <li class="active">
                        <a href="javascript:;">Cargo</a>
                    </li>
                </ol>
            </div>

        <div class="container-fluid-md">
            <a class="btn btn-inverse" href="{{ path('cargo_index') }}">Voltar</a>            

            <a class=" btn btn-primary" href="{{ path('cargo_edit', {'id': cargo.id}) }}">Editar</a>            
            <br></br>
            {{ include('cargo/_delete_form.html.twig') }}

            

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cargo.id }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ cargo.descricao }}</td>
            </tr>
        </tbody>
    </table>
    
    </div>

{% endblock %}
